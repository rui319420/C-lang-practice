#include <stdio.h>

int main(void)
{
	int N;
	int Q;

	scanf("%d %d", &N, &Q);

	int A[N];
	int i = 0;
	while (i < N)
	{
		scanf("%d", &A[i]);
		i++;
	}
	int min[Q];
	i = 0;
	while (i < )
	return 0;
}